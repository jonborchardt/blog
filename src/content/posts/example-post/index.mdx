---
title: Example Post
description: A temporary post proving the content pipeline — frontmatter, MDX, colocated assets, series, tags, and a React island.
publishedAt: 2026-08-17
series: example-series
seriesOrder: 1
tags: [meta, engineering]
draft: false
---

import Callout from "@/components/blog/Callout.astro";
import ExampleInteractive from "./components/ExampleInteractive";
import example from "./example.svg";

This post exists to prove the scaffold works. It will be deleted.

## Static content stays static

Prose, headings, and code render to plain HTML with **no client JavaScript**.

```ts
export const alwaysShippable = true;
```

<Callout title="Shared component">
  This callout is an Astro component from `src/components/blog/`. Zero JS shipped.
</Callout>

## Colocated assets

An SVG rendered with a plain `<img>`:

<img
  src={example.src}
  width={example.width}
  height={example.height}
  alt="A grey circle labelled SVG"
/>

A raster image via Markdown syntax gets responsive `srcset`, dimensions and lazy loading:

![A schematic card on a grid background standing in for a real screenshot](./pipeline.png)

## Tables

| Check                     | Where it runs   | Fails the build? | Notes                                        |
| ------------------------- | --------------- | ---------------- | -------------------------------------------- |
| Frontmatter schema        | content loader  | yes              | Zod, `src/content.config.ts`                 |
| Unknown series/tags       | content loader  | yes              | registries in `src/config/`                  |
| Duplicate or reserved     | `validatePosts` | yes              | every page that lists posts                  |
| axe WCAG 2.2 AA           | e2e             | yes              | every sitemap URL, in the production preview |
| Horizontal overflow @360w | e2e             | yes              | every sitemap URL                            |

## React island

Only the component below hydrates. Everything else on the page is static.

<ExampleInteractive client:visible />
