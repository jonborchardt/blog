---
title: Google Charts to D3
description: In 2013 I replaced our off-the-shelf Google charts with hand-built D3 dashboards where dragging the timeline refilters the whole page.
publishedAt: 2026-08-20
series: past-work
seriesOrder: 7
tags: [d3, data-viz, adtech, retrospective]
authority: 'E:\github2\Labs'
hero:
  src: ./hero.png
  alt: A timeline strip with a drag window covering roughly November 2010 through June 2012, above a ranked bar list of TV networks led by FNEW, two spend histograms and a US county map, all showing only the data inside that window
draft: false
---

import Callout from "@/components/blog/Callout.astro";
import Figure from "@/components/blog/Figure.astro";
import before from "./before-google-chart.png";
import trackerRest from "./tracker-rest.png";
import trackerSwept from "./tracker-swept.png";
import sweep0 from "./sweep-0-rest.png";
import sweep1 from "./sweep-1.png";
import sweep2 from "./sweep-2.png";
import sweep3 from "./sweep-3.png";
import sweepVideo from "./rotations-sweep.webm";
import SweepDemo from "./components/SweepDemo.astro";
import FrameScrubber from "./components/FrameScrubber.astro";

In 2013 I was the only UI developer at PrecisionDemand, a TV ad-tech startup, which meant every
chart our customers looked at was a chart I had put there. Most of them were not mine. They were
Google's, loaded off `google.com/jsapi`, configured with a few lines and shipped. That year I spent
my 20% time replacing them with D3 charts I wrote myself, and the folder of demos that came out of
that is still sitting in [the portfolio I froze in 2018](/labs-2018-snapshot/).

This is the post about that swap: what the off-the-shelf chart did, what I built to replace it, and
the one interaction, dragging a window across a timeline and watching an entire page of charts
recompute, that turned out to matter far more than the charts did.

## The chart I was replacing

Two files still sit unlinked at the root of the portfolio, `fordsuvindustry.htm` and
`jonbtest.htm`. They are the "before" artifacts, and they are about as simple as a 2013 chart page
got:

```html
<script type="text/javascript" src="http://www.google.com/jsapi"></script>
<script type="text/javascript">
  google.load("visualization", "1", { packages: ["annotatedtimeline"] });
  google.setOnLoadCallback(drawChart);
</script>
```

Under that, a couple of thousand hardcoded `data.addRows([...])` lines: a date, two targeting-score values,
and four spare string columns per row, an annotation title and its body text for each of the two
series. A number our own modelling produced was being drawn by a component I did not write and
could not change.

<Figure
  caption="fordsuvindustry.htm, the Google annotated timeline the D3 work replaced. Every advertiser, network and number in this archive is demo data."
  width="wide"
  zoom
>
  <img
    src={before.src}
    width="1400"
    height="505"
    alt="A Google annotated timeline of Ford SUV targeting scores, with zoom preset buttons reading 1h 1d 5d 1w 1m 3m 6m 1y max, a blue Ford line and a red Industry Overall line from 2010 to 2013, lettered markers A to E on the lines, a range-selector strip along the bottom, and a panel on the right listing each annotation with its date"
  />
</Figure>

It is a good chart. Two series, lettered annotations pinned to dates with a panel explaining them,
zoom presets, and a range selector along the bottom. In 2013 Google drew all of that with a Flash
object, which is the detail that explains the headline on the page I replaced it with: "Replacement
for existing Flash based Industry Tracker". The Flash in that sentence was never ours. It was
Google's.

<Callout variant="note" title="These pages were supposed to be dead">
  I went in expecting a blank screen. The old `jsapi` loader is long deprecated and I had written
  these two pages off. They render fine. `google.load` now forwards to
  `gstatic.com/charts/51/loader.js`, which quietly draws the modern SVG `annotationchart` instead:
  twelve `<svg>` elements on the page and not one `<object>` or `<embed>`. So the screenshot above is
  Google's replacement for its own Flash chart, standing in for the thing I was actually looking at
  in 2013.
</Callout>

## Building the equivalent

The interview question I was asked about this work was whether the demo set won the argument. I
don't remember an argument, and I am not going to invent one. What I have is the files, and the
files are unusually clear about the intent: this was not a chart inspired by Google's, it was a copy
of Google's with the ceiling taken off.

The replacement chart is `nv.models.PdLineWithFocusChart`, 795 lines registered straight into NVD3's
model namespace. `Pd` for PrecisionDemand. Two siblings sit next to it, `PdBubbleMap` and
`PdHeatMap`. And the data file it eats gives the game away:

```json
{
  "date": 1270105200000,
  "value": -0.044918704,
  "label": "A",
  "description": "A mixed bag of winners and losers",
  "notes": "DISC (Dirty Jobs & Mythbusters), DIY, NFLN, SPD and TRAV are well targeted, ..."
}
```

A label, a headline and a body per point. The label is new: Google auto-lettered its own markers,
there was no column for it. The other two are a rename of Google's pair: `description` is Google's
title column, and `notes` is Google's text column, which the Ford data mostly left unused. The series
colours are pinned in the same
file to `#4684EE` and `#DC3912`, the same blue and the same red the Google chart drew. Someone
opening the new page was not supposed to notice a new chart library. They were supposed to notice it
did more.

<Figure
  caption="The D3 replacement, with its own annotation panel and a focus/context strip in place of Google's zoom presets."
  width="wide"
  zoom
>
  <img
    src={trackerRest.src}
    width="1400"
    height="789"
    alt="A page headed Replacement for existing Flash based Industry Tracker, showing a blue GM SUVs line and a red Non-GM SUVs line from 2010 to 2013 with lettered markers B to E, a full-width context strip of the same two lines below it, and a panel on the right listing five annotations from a mixed bag of winners and losers through to back to a mix of good and bad"
  />
</Figure>

The zoom presets are gone, replaced by a full-width strip of the same two lines underneath the main
chart. You drag a window across that strip and the chart above it becomes that window. It is a
smaller idea than "1h 1d 5d 1w 1m 3m 6m 1y max" and a much better one, because the window is
continuous and you can slide it rather than pick it off a menu.

## The demo set

The tracker was one page of five. `demos/d3Demo` holds `industryTracker`, `rotationExample`,
`programExample`, `explorerExample` and `webSpike`, plus two hub pages. The hubs are the part I had
forgotten and now like the most. `demos.html` is six escalating throwaways (hover, animation on
click, more advanced animation, charting, animated charts, thousands of animated points), the
sketchbook of somebody learning a library in public. And `index.html`, titled "Front-end data
visualization examples", is not a demo at all. It is a page of links: d3js.org, nvd3.org,
crossfilter, four New York Times interactives, Bostock's Wealth & Health of Nations. That page is
the actual pitch. The demos underneath it are the evidence I could build that.

The stack under the demos was D3 v3 with NVD3 for the chart models, Square's crossfilter for the
filtering, dc.js on the explorer page, and topojson for the maps. `webSpike.html` is a static twin
of an ASP.NET explorer whose backing API has been switched off for years, and is now the only way to
see that tool at all. One file, `crossfilterChart.js`, opens with a comment I am still glad I wrote:

```js
// A direct copy from the crossfilter example:
// https://github.com/square/crossfilter/blob/50227903a5567d24c0d38141a5e94b5dec932b4c/index.html
```

## The sweep

Here is the interaction the whole thing was really about.

Crossfilter holds one flat table in memory (for the rotations page, one row per TV rotation buy) and
lets you declare dimensions over it: purchase date, network, commission rate, creative length,
geography. Every chart on the page is a group over one of those dimensions. Filter any dimension and
every other group recomputes, incrementally, fast enough that you can do it inside a `mousemove`.

So the timeline strip is not a zoom control. It is a filter, and dragging it refilters the page.
These four frames, shot this month, are one continuous drag across the strip on
`rotationExample.html`, captured mid-gesture with the mouse button still down.

<Figure
  caption="At rest: all buys from 2010 to 2013. HIST tops the network list at 7,494,926.60."
  width="wide"
  zoom
>
  <img
    src={sweep0.src}
    width="1400"
    height="906"
    alt="A crossfilter dashboard showing rotation purchases from 2010 to 2013, an unfiltered context strip below it, and a ranked network list led by HIST at 7,494,926.60 and FNEW at 6,601,245.00, beside a commission-rate histogram spread across eight rates and a US county map with four circles"
  />
</Figure>

<Figure
  caption="A narrow window over the winter of 2010 to 2011. The ranking is now led by GALA at 359,290.00, a network that did not appear in the top five a moment ago."
  width="wide"
  zoom
>
  <img
    src={sweep1.src}
    width="1400"
    height="906"
    alt="The same dashboard with a narrow drag window over late 2010 to early 2011 on the context strip, the main chart rescaled to that window, and the network list now led by GALA at 359,290.00 and FNEW at 209,600.00, with the commission histogram collapsed onto the 9 percent and 10 percent bars"
  />
</Figure>

<Figure
  caption="Widen to roughly nine months and FNEW takes the lead at 714,225.00, with AMC edging GALA by just 190."
  width="wide"
  zoom
>
  <img
    src={sweep2.src}
    width="1400"
    height="906"
    alt="The same dashboard with the drag window widened to roughly November 2010 through August 2011, the network list led by FNEW at 714,225.00 followed by AMC at 606,120.00 and GALA at 605,930.00, and the map circles in different places than the previous frame"
  />
</Figure>

<Figure
  caption="Nineteen months in the window and the totals are four times larger again: FNEW 2,881,495.00, HIST back up to second."
  width="wide"
  zoom
>
  <img
    src={sweep3.src}
    width="1400"
    height="906"
    alt="The same dashboard with the drag window covering roughly November 2010 through June 2012, the network list led by FNEW at 2,881,495.00 and HIST at 1,846,784.60, the commission histogram dominated by the 9 percent bar and the creative-length histogram split between 30 and 60 seconds"
  />
</Figure>

Nothing on that page is a page load. The network ranking reorders, the commission-rate histogram
collapses onto two bars and spreads back out, the creative-length split flips between 30-second and
60-second spots, and the circles on the county map move, all of it while the mouse button is still
down.

The tracker gets the same treatment. Sweep its strip and the annotations stay pinned to their dates
while the window closes around them:

<Figure
  caption="The tracker mid-sweep: the window has closed to mid-2010 through late 2011 and annotation C has come along for the ride."
  width="wide"
  zoom
>
  <img
    src={trackerSwept.src}
    width="1400"
    height="789"
    alt="The industry tracker with a drag window over mid-2010 to late 2011 on its context strip, the main chart rescaled so only that period is shown with annotation marker C visible on the blue line, and the same five-item annotation panel unchanged on the right"
  />
</Figure>

If you want to feel this rather than read about it, the dc.js homepage still hosts the canonical
public version of the same interaction model: [dc.js](https://dc-js.github.io/dc.js/). Click a slice
or sweep a timeline there and every other chart on the page follows.

## Interlude: four ways to show you a sweep

I am not happy with any of my attempts to put this interaction on a page, so this section is honest
about being an experiment: the same sweep, presented four ways. One of them will survive a future
edit; the others will not.

**Option 1** is what you just scrolled through — captured frames with captions. It needs no
JavaScript and it reads in RSS, and it is also exactly the thing this whole post complains about:
describing motion instead of showing it.

**Option 2** is a recording. This is the real `rotationExample.html`, driven this month — the brush
window drawn, then grabbed and slid back and forth while every chart follows:

<Figure caption="The sweep, recorded: the brush is drawn across the strip and then dragged, and the focus chart, network ranking, histograms and county map all refilter while the button is down." width="wide">
  <video
    src={sweepVideo}
    controls
    muted
    playsinline
    preload="metadata"
    poster={sweep0.src}
    width="1280"
    height="860"
    aria-label="Screen recording of the rotations dashboard: a brush window is drawn on the timeline strip and dragged left and right; the spend line rescales, the network ranking reorders, the commission histogram collapses and spreads, and the county-map circles move during the drag"></video>
</Figure>

**Option 3** is the real thing, rebuilt small. Not a recording, and not the 2013 code — it is
crossfilter and dc.js, the same libraries at their current versions, running in this page over two
thousand synthetic rotation buys. Drag across the lower strip; the count above the charts is
crossfilter doing set intersection under your cursor:

<SweepDemo />

**Option 4** keeps the captured pixels but gives you back the gesture: the four frames from option
1 under a slider.

<FrameScrubber
  frames={[
    {
      src: sweep0.src,
      label: "At rest: 2010 to 2013, HIST leads.",
      alt: "A crossfilter dashboard showing rotation purchases from 2010 to 2013, an unfiltered context strip below it, and a ranked network list led by HIST at 7,494,926.60",
    },
    {
      src: sweep1.src,
      label: "A narrow winter window: GALA leads.",
      alt: "The same dashboard with a narrow drag window over late 2010 to early 2011, the network list led by GALA at 359,290.00",
    },
    {
      src: sweep2.src,
      label: "Nine months: FNEW takes the lead.",
      alt: "The same dashboard with the window widened to roughly November 2010 through August 2011, led by FNEW at 714,225.00",
    },
    {
      src: sweep3.src,
      label: "Nineteen months: FNEW 2,881,495.00.",
      alt: "The same dashboard with the window covering roughly November 2010 through June 2012, led by FNEW at 2,881,495.00 and HIST second",
    },
  ]}
/>

## What it turned into

The charts themselves did not survive. D3 v3, NVD3 and a hand-rolled `nv.models.Pd...` are not how I
would draw this today, and the pages that used them were rewritten more than once after AOL acquired
us in May 2014 and I moved the product off Silverlight and onto AngularJS.

The interaction survived. When I ask myself which of these side projects went anywhere, the list is
the Audience Planner, DocMapper, sell-side targeting, and then, oddly for a list of products, "using
crossfilter and dc.js for highly interactive charting". The technique itself became flagship. The
industry-ratings and sell-side dashboards that real customers used were built on this exact model:
click anything to filter everything, drag the timeline to refilter live. Those are their own posts.

## How it holds up in 2026

It runs. `npx http-server . -p 8123 -c-1`, open `/demos/d3Demo/rotationExample.html`, and the sweep
above is what you get. Those frames are from this month, not from 2013. The caveats are the ones you
would expect of a thirteen-year-old page:

- D3 and topojson load from `http://d3js.org`, so the demos work over a local `http` server and
  would break as mixed content on any `https` rehost.
- The sweep works; some of the other interactions are flaky now, and I have left them that way.
- There is an earlier, rougher pass at the same tracker in a separate `industryTracker` folder,
  unlinked from the site root, along with a dated July 2013 GM/Cadillac build. The polished copies
  are the ones in `demos/d3Demo`.

The Google chart still renders too, which I did not expect. But it renders because Google kept
maintaining it and silently swapped its guts out from under my page. Mine renders because, once it
was written, it never needed anything from anyone.
