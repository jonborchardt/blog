---
title: "ONE TV: One Design Language, Three Generations"
description: AOL's ONE TV had an upfront half and a scatter half, and behind both a constraints dialog I had first drawn in Balsamiq in 2012.
publishedAt: 2026-08-20
series: work-2001-2018
seriesOrder: 11
tags: [retrospective, adtech, data-viz]
authority: 'E:\github2\Labs'
hero:
  src: ./hero.png
  alt: A ONE TV scenario grid comparing budget, spend, CPM and scores, beside a filter rail of checkboxes
draft: false
---

import { Image } from "astro:assets";
import Callout from "@/components/blog/Callout.astro";
import Figure from "@/components/blog/Figure.astro";
import VizFigure from "@/components/blog/VizFigure.astro";
import Lineage from "./constraints-lineage.svg";
import upfrontWireframe from "./upfront-wireframe.png";
import upfrontShipped from "./upfront-shipped.png";
import scatterWireframe from "./scatter-wireframe-2012.png";
import scatterSilverlight from "./scatter-silverlight-2013.png";
import scatterShipped from "./scatter-shipped-2018.png";

AOL bought PrecisionDemand in May 2014, and I spent the four years after that on one product. First
as the senior engineer leading the migration off Silverlight and onto AngularJS, then, from June
2016, as a Principal running a UI team of three and owning everything a user saw on the Linear TV
advertising platform. The platform was called ONE TV.

## Upfront: a quarter, planned all at once

The shipped upfront screen is a scenario comparison. You pick a campaign, a date range, a network
group and a demographic, and then you make as many plans as you like against that one budget and
put them side by side.

<Figure
  caption="The shipped upfront optimizer, early 2018, with me logged in."
  width="wide"
  zoom
>
  <Image
    src={upfrontShipped}
    width={1426}
    height={988}
    alt="The Optimize Upfront screen: campaign fields above a scenario grid and a row of summary charts"
  />
</Figure>

Each scenario runs the optimizer with a different objective (shift the buy, decrease the budget),
and the grid ranks the results by what they cost and how well they hit the target. By 2018 there were
three scoring columns on that grid, MVS, MQS and SMC, three ways of scoring the same plan. I
couldn't tell you today what all three of those initialisms stood for.

## Scatter: the tactical half

Scatter is the other half of the year. The upfront market sells a quarter or a season in advance;
scatter is the tactical buying that happens inside the quarter, once budgets have moved and there's
inventory left over to place against.

<Figure caption="The shipped scatter screen, 2018." width="wide" zoom>
  <Image
    src={scatterShipped}
    width={1390}
    height={657}
    alt="The Black Opium campaign screen: tactic settings, a forecasted plan strip and a network table"
  />
</Figure>

A tactic group inside a campaign, a budget of \$390,000, a forecast of \$387,003 against it, and a
headline targeting score of 1.26. Underneath, the plan broken out by network with a score
each: USA at 1.13, Oxygen at 1.86, E! at 1.38, Bravo at 1.21, and CNBC and NBC both down at 0.58.
It's a ranking of two completely different pieces of inventory on how efficiently they reach the
people you're paying to reach.

## The same dialog, three generations deep

Balsamiq first, then code. That was how I worked at PrecisionDemand and it was still how I worked
at AOL: draw the whole screen, annotate the behaviour on it in callouts, argue about the drawing,
then build the drawing.

<Figure
  caption="The upfront design, wireframed and annotated before it was built. The mockup's address bar reads onetv.aol.com."
  width="wide"
  zoom
>
  <Image
    src={upfrontWireframe}
    width={2036}
    height={1661}
    alt="A hand-annotated Balsamiq wireframe of the upfront screen with yellow callout notes on each panel"
  />
</Figure>

The callouts are the specification. "Creating a scenario automatically adds a new column to the grid
below (and a new series in all charting)." "All charts show ALL scenarios AND all update on every
change." "Ready to print for sending to network", because the end of all this optimizing was a
piece of paper that told a network which spots to buy and which to sell back.

The wireframe is a Q3 campaign against FOX Networks Group at \$1.25M, 21% of the total. The shipped
screen is a Q2 campaign against Univision at \$2.65M. Different quarter, different networks, same
screen.

<Figure
  caption="Three campaign-planner dialogs from the PrecisionDemand era, wireframed in Balsamiq. The mock milestone log inside is dated January 2012."
  width="wide"
  zoom
>
  <Image
    src={scatterWireframe}
    width={3074}
    height={1399}
    alt="Three Balsamiq wireframes: Edit Campaign, Edit Cell and Edit Cell Week dialogs with callout notes"
  />
</Figure>

Here is what it looked like a year later, after it had been built as a Silverlight dialog, in the
product, with a real edit timestamp on it:

<Figure caption="The same constraints, shipped in Silverlight. Last edit: 29 January 2013.">
  <Image
    src={scatterSilverlight}
    width={479}
    height={371}
    alt="A Silverlight dialog titled Edit Campaign Constraints with numeric fields for each optimizer limit"
  />
</Figure>

And here is the Scenario Editor from the ONE TV upfront wireframe above, four years after that, at
a different company, on a different frontend stack, after an acquisition: the same list of fields,
in the same order, with the same default values.

<VizFigure
  name="The constraints dialog across three generations, 2012 to 2017"
  summary="The same five optimizer constraints appear in the same order in a 2012 Balsamiq wireframe, a 2013 shipped Silverlight dialog and a 2017 ONE TV wireframe. The 2012 and 2017 defaults are identical (0.30, 0.20, 0.05, 50, 3.00) while the 2013 build shipped different ones: stricter score floors and a much looser cap on any single buy. The only wording change in five years is program becoming airing."
  data={{
    caption: "Constraint fields and their default values in each generation",
    columns: ["Field", "2012 wireframe", "2013 Silverlight", "2017 wireframe"],
    rows: [
      ["Min program / airing cluster score", "0.30", "0.50", "0.30"],
      ["Min program / airing blended score", "0.20", "0.50", "0.20"],
      ["Max single program / airing buy %", "0.05", "40.00 %", "0.05"],
      ["Max single program / airing count", "50", "45", "50"],
      ["Min program / airing CPM", "3.00", "$1.00", "3.00"],
    ],
  }}
>
  <Lineage class="mx-auto h-auto w-full max-w-2xl" aria-hidden="true" />
</VizFigure>

Minimum cluster score, minimum blended score, maximum single buy percentage, maximum single
count, minimum CPM per unit. Five years, one rename: what had been a **program** became an
**airing**, which is a more precise word for the same thing. The numbers in the boxes did move: the
2013 build shipped stricter score floors than the drawing asked for and a far looser cap on any
single buy, which is what happens when a design meets real inventory. But the fields, and their
order, are the fields.

I don't think anyone decided this. Nobody wrote down a design language and made us follow it. The
vocabulary was useful enough that every rewrite kept it rather than paying to re-derive it, and by
2017 I was drawing wireframes against a list of constraints I'd first drawn in 2012, mostly out of
muscle memory.

## What happened to it

AOL renamed the division Oath in June 2017: same team, same platform, a new logo on the same
screens. I left in March 2018, and the reason these screenshots exist at all is that I was taking
them on the way out, for a portfolio. ONE TV was shuttered after I left.

So the product is gone, and what survived it is a directory listing where a 2012 drawing, a 2013
dialog and a 2018 web app all sit next to each other, and you can't tell from the field labels
which is which.
