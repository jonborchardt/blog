---
title: "Upscaling Sierra SCI0 PIC: the native render oracle"
description: How snap-blend upscales Sierra SCI0 vector backgrounds without leaking flood fills, using a native-resolution render as the oracle for every scaled fill.
publishedAt: 2026-08-18
updatedAt: 2026-08-19
series: sierra-games
seriesOrder: 1
tags: [engineering, graphics, gamedev, retrocomputing, typescript]
authority: 'F:\github\sci.js'
hero:
  src: ./hero.png
  alt: A small pixel-grid diamond with a bisecting line, sealed, beside a 3x scaled copy of the same diamond where a one-pixel gap at the junction lets the fill colour spill into the other half, with an oracle badge between them asking whether the scaled fill area is nine times the native one
draft: false
---

import Callout from "@/components/blog/Callout.astro";
import Figure from "@/components/blog/Figure.astro";
import VizFigure from "@/components/blog/VizFigure.astro";
import GapClosure from "./gap-closure.svg";
import SnapBlendPipelineViz from "./snap-blend-pipeline-viz.svg";
import OverlayStackViz from "./overlay-stack-viz.svg";
import DitherBlend from "./dither-blend.svg";
import sweepOriginalScale6x from "./sweep-original-vs-scale6x.svg";
import sweepScale6xSnap from "./sweep-scale6x-vs-snap-blend.svg";
import sweepNativeSnap from "./sweep-native-vs-snap-blend.svg";
import roomJunkPile from "./sweep-room-junk-pile.svg";
import roomTunnels from "./sweep-room-tunnels.svg";
import roomRobotHead from "./sweep-room-robot-head.svg";
import roomHangar from "./sweep-room-hangar.svg";
import roomScrapyard from "./sweep-room-scrapyard.svg";
import roomCockpit from "./sweep-room-cockpit.svg";
import roomAstroChicken from "./sweep-room-astro-chicken.svg";
import roomBurgerInterior from "./sweep-room-burger-interior.svg";
import roomBurgerExterior from "./sweep-room-burger-exterior.svg";
import roomDesert from "./sweep-room-desert.svg";
import sweepAi from "./sweep-original-vs-ai-upscale.svg";

Sierra On-Line's SCI0 engine draws backgrounds as vector commands — lines, patterns, and flood fills executed in sequence at 320x190. It works perfectly at native resolution. But render those same commands at 2x, 3x, or higher? Fills leak through hairline gaps that didn't exist at 320 pixels wide, and your pristine space bar becomes a watercolor disaster.

This is the problem **snap-blend** solves.

## The problem

SCI0 PICs aren't rasterized images — they're programs. A PIC resource is a stream of drawing commands: draw a line here, fill this region there. The fill commands depend on boundaries being watertight. At native resolution, the integer-coordinate Bresenham lines form perfect barriers.

![A PIC resource opened in an editor: a long list of Line commands with integer coordinates on the left, and the wireframe outline of a crashed spaceship those lines draw on the right](./pic-command-stream.png)

Scale everything up and suddenly there are fractional-pixel gaps where fill ink escapes into neighboring regions, corrupting the image. Scaling can also fragment boundaries, causing fills to terminate early or leave holes.

A simplified PIC command stream looks something like this:

```plaintext
SET_VISUAL_COLOR 0x03        // dark cyan
// Diamond
DRAW_LINE (160, 40) → (240, 95)
DRAW_LINE (240, 95) → (160, 150)
DRAW_LINE (160, 150) → (80, 95)
DRAW_LINE (80, 95) → (160, 40)
// Line intended to bisect the Diamond
DRAW_LINE (120, 68) → (200, 123)
FILL (160, 95)               // flood fill stays bounded
```

At 320x190, the rasterized lines happen to touch cleanly, so the shape remains sealed and the fill stays inside.

Scale the same geometry to 960x570, however, and the independently rounded midpoint coordinates may no longer land exactly on the scaled diagonal edges. That can create a one-pixel gap at the junction, allowing the flood fill to escape and flood the entire canvas.

![Two cyan diamonds with a bisecting line on black. Left, at 320x190, the pixel lines meet cleanly so the fill stays inside. Right, scaled 3x to 960x570, an orange circle marks a one-pixel gap where the bisecting line misses the edge, so the fill leaks outside](./diamond-gap-native-vs-3x.png)

Existing solutions don't help. ScummVM and SCI Companion render at native res. Pixel-art upscalers (Scale2x, hqx, xBR) operate on finished rasters — they can't fix a fill that leaked during rendering. The problem has to be solved _inside_ the renderer.

Upscaling after rasterization preserves correctness, but introduces uneven line thickness and edge wobble.

<Figure caption="Original 320x190 render versus a Scale6x post-raster upscale of the same Space Quest 3 room; the sweep moves back and forth between the two." zoom>
  <img
    src={sweepOriginalScale6x.src}
    width="1920"
    height="1140"
    alt="Sweep comparison of a Space Quest 3 room, a crashed ship in a blue hangar with junk piles: the original 320x190 render on the left, a Scale6x upscale on the right, with wobbly uneven outlines in the upscaled half"
  />
</Figure>

Snap-blend fixes this by altering the rendering directly.

<Figure caption="Scale6x versus snap-blend on the same room: straight, evenly weighted lines and the same regions." zoom>
  <img
    src={sweepScale6xSnap.src}
    width="1920"
    height="1140"
    alt="Sweep comparison of the same hangar room: Scale6x on the left with wobbly outlines, snap-blend on the right with clean straight one-pixel lines and identical fill regions"
  />
</Figure>

## The approach

**snap-blend** combines several techniques into a pipeline: a native-resolution pass first, then a scaled pass that the native result supervises fill by fill.

<VizFigure
  name="The snap-blend rendering pipeline"
  summary="The PIC command stream is rendered once at native resolution to record how many pixels each FILL touches. The same commands are then rendered at the target scale: lines get snapped endpoints, and before each fill a micro-gap pass closes diagonal gaps. Each fill runs under a guard — if it exceeds five times its native count times scale squared, or forty percent of the screen, it is rolled back mid-fill. A fill that ends with zero pixels while its native count is non-zero is recovered by blitting the nearest-neighbour scaled native mask; undershoots pass through untouched. A final dither-to-RGBA blend runs over the whole canvas."
>
  <SnapBlendPipelineViz class="mx-auto h-auto w-full max-w-2xl" aria-hidden="true" />
</VizFigure>

### 1. Endpoint snapping and seed nudging

Line endpoints snap to nearby **already-drawn boundary pixels**: a scan around the scaled endpoint, under a small distance budget and a directional cone, finds an existing pixel to land on, so scaled lines actually touch the boundaries they were drawn to meet.

```plaintext
function snapEndpoint(x, y):
    // scan a small radius around the scaled endpoint for an
    // already-drawn pixel, within a distance budget and a
    // directional cone; snap to the closest hit, else keep (x, y)
```

Fill seeds get the mirror treatment. At scale, boundary lines are thick enough to cover a seed's coordinates, and a fill seeded on a boundary paints zero pixels — the single biggest failure class the oracle exposed. Seeds are nudged off boundaries before the fill runs.

### 2. Diagonal micro-gap closure

A morphological pass detects and closes single-pixel diagonal gaps that form at scaled line junctions. These are the most common leak source.

```plaintext
// scan for diagonal gaps in the boundary mask:
//   . X      X .
//   X .  or  . X
// if found, fill one pixel to close the gap
for each pixel (x, y) in boundaryMask:
    if isEmpty(x, y) AND isBoundary(x-1, y-1) AND isBoundary(x+1, y+1):
        boundaryMask[x, y] = FILLED
    if isEmpty(x, y) AND isBoundary(x+1, y-1) AND isBoundary(x-1, y+1):
        boundaryMask[x, y] = FILLED
```

The pass is a two-pixel test: an empty cell whose diagonal neighbours are both boundary gets filled, which is enough to seal the junction.

<Figure caption="One diagonal micro-gap before and after closure. Left, a flood fill leaks through the empty cell between two diagonal boundary pixels; right, that one cell is filled and the fill stays on its side.">
  <VizFigure
    name="Diagonal micro-gap closure on a 6 by 6 pixel grid"
    summary="Two grids with a diagonal boundary that has one missing pixel. In the left grid the flood fill has spread through the gap to both sides of the diagonal; in the right grid the gap pixel is filled and the fill covers only the region above the diagonal."
  >
    <GapClosure class="h-auto w-full max-w-xl" aria-hidden="true" />
  </VizFigure>
</Figure>

### 3. Native render oracle

The renderer first builds a native-resolution reference render and uses it to validate each scaled flood fill. Instead of predicting where scaling errors might occur, snap-blend renders once at native resolution and uses that output as ground truth.

```plaintext
// Phase 1: render at native res to build the oracle
nativeCanvas = render(picCommands, scale=1)
for each FILL command[i]:
    expected[i] = countPixelsFilled(nativeCanvas, command[i])

// Phase 2: render at target scale, supervised by the oracle
scaledCanvas = createCanvas(320 * scale, 190 * scale)
for each command in picCommands:
    if command is DRAW_LINE:
        drawSnappedLine(scaledCanvas, command, scale)
    if command is FILL:
        closeGaps(scaledCanvas)  // micro-gap closure pass
        limit = expected[i] * scale * scale * MAX_OVERSHOOT  // 5.0
        actual = guardedFloodFill(scaledCanvas, command, limit)
        // the guard aborts and unwinds the fill mid-flood if it
        // passes the limit, or 40% of the whole screen

        if actual == 0 AND expected[i] > 0:
            // rolled back, or the seed never produced a pixel
            blitNativeMask(scaledCanvas, nativeCanvas, command, scale)
        // undershoots are kept as-is
```

If a fill blows far past its native-predicted area, it's a leak — the guard unwinds it mid-flood rather than letting it flood the canvas. The native render acts as an oracle supervising the upscaled render. Undershoots are tolerated: thicker upscaled lines eat fill area by construction, so a somewhat-small fill is expected physics at scale, not a leak.

### 4. Native-mask fallback

When a fill ends with zero pixels — rolled back by the guard, or its seed never produced one — while the native render says the region should have some, the corresponding region from the native render is nearest-neighbor-scaled and composited in. Correctness is preserved at the cost of sharpness in that region — but in practice, fallback fires rarely.

```plaintext
function blitNativeMask(scaledCanvas, nativeCanvas, fillCmd, scale):
    // find the filled region in the native render
    mask = extractFillRegion(nativeCanvas, fillCmd)
    // nearest-neighbor upscale the mask
    upscaled = nearestNeighborScale(mask, scale)
    // composite into the scaled canvas
    composite(scaledCanvas, upscaled, fillCmd.color)
```

### 5. Dither-to-RGBA blending

A cosmetic post-process. SCI0 uses dithered patterns to simulate colors beyond the 16-color EGA palette. The blend pass resolves these into smooth RGBA values for the browser renderer.

```plaintext
// SCI0 stores each pixel's dither pair in its byte: (high << 4) | low
// e.g. 0x3b = cyan (0x3) + light cyan (0xb), drawn as an
// alternating checkerboard at native resolution
//
//   C L C L        blended:
//   L C L C   →    every 0x3b pixel becomes the same
//   C L C L        perceptual mix of the pair
//   L C L C

// No pattern detection needed — the byte itself names both colours.
for each pixel byte in output:
    rgba = BLEND_TABLE[byte]   // precomputed okLab mix of the pair
```

<Figure caption="The blend pass in one block: a checkerboard of two EGA palette entries becomes pixels of their perceptual mix. Region shapes are untouched.">
  <VizFigure
    name="Dither checkerboard blended to a single RGBA colour"
    summary="Left, a 4 by 4 pixel checkerboard alternating two colours, with one 2 by 2 block outlined; right, a solid square of the perceptual mix of the two colours. The output keeps the same footprint and loses only the checkerboard."
  >
    <DitherBlend class="h-auto w-full max-w-xl" aria-hidden="true" />
  </VizFigure>
</Figure>

This produces output that looks cleaner on modern displays without altering the underlying data.

## What's novel

None of the individual techniques are new. The interesting part is combining them into a supervised rendering pipeline where the native render validates the scaled one.

I haven't seen this approach used in retro vector renderers before. The problem domain is niche, but the pattern generalizes. Any format where vector drawing commands produce resolution-dependent fill boundaries could benefit: [AGI-era Sierra games](/agi-up-as-a-reference-renderer/), LucasArts SCUMM backgrounds, or other retro engines with interleaved draw/fill command streams.

## Results

Across the test corpus at 6x, snap-blend produced no catastrophic fill floods: escapes are caught mid-fill, rolled back, and recovered from the native mask. The flagged fills that remain are overwhelmingly small boundary-thickness undershoots, plus a handful of guarded micro-gap escapes the rollback contains. The blend pass is purely cosmetic — same correctness, nicer pixels.

It's the production strategy — registered as `snap-v5-blend` — for my sci.js project, a TypeScript reimplementation of the SCI0 engine focused on making Space Quest 3 playable in the browser with upscaled visuals, built on [32bitkid's `@4bitlabs` decoding libraries](https://github.com/32bitkid/sci.js).

Each comparison below sweeps between the two renders of one Space Quest 3 room; the labels in the bottom corners say which is which.

<Figure caption="Native 320x190 render versus snap-blend at 6x: identical regions, cleaner lines." zoom>
  <img
    src={sweepNativeSnap.src}
    width="1920"
    height="1140"
    alt="Sweep comparison of a Space Quest 3 room, an alien landscape with a crashed ship: native render on the left, snap-blend at 6x on the right, with the same fill regions in both"
  />
</Figure>

<Figure caption="Original versus snap-blend: junk pile with a magenta hull section and cyan arch." zoom>
  <img
    src={roomJunkPile.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: a scrap heap with a magenta hull section in the foreground, a cyan arch, and orange rock in the background"
  />
</Figure>

<Figure caption="Original versus snap-blend: sewer tunnels under a blue-lit ceiling." zoom>
  <img
    src={roomTunnels.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: three cyan-ringed tunnel mouths in a red and blue cavern with a small ship in the corner"
  />
</Figure>

<Figure caption="Original versus snap-blend: giant robot head among the wreckage." zoom>
  <img
    src={roomRobotHead.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: a huge magenta and grey robot head with a green gem on the right, red and cyan hull pieces on the left"
  />
</Figure>

<Figure caption="Original versus snap-blend: ship in a blue hangar bay." zoom>
  <img
    src={roomHangar.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: a green and cyan ship suspended in a blue-lit hangar with rubble on the floor"
  />
</Figure>

<Figure caption="Original versus snap-blend: scrapyard with a wrecked pod and yellow tubes." zoom>
  <img
    src={roomScrapyard.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: a scrapyard littered with blue debris, a wrecked white pod, and yellow tubes and pipes"
  />
</Figure>

<Figure caption="Original versus snap-blend: ship cockpit interior." zoom>
  <img
    src={roomCockpit.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: a symmetrical spaceship cockpit in grey and magenta with a round viewport and a pilot chair"
  />
</Figure>

<Figure caption="Original versus snap-blend: the Astro Chicken arcade cabinet." zoom>
  <img
    src={roomAstroChicken.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: an arcade screen reading Astro Chicken by ScumSoft with a cartoon chicken"
  />
</Figure>

<Figure caption="Original versus snap-blend: inside Monolith Burger." zoom>
  <img
    src={roomBurgerInterior.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: the interior of a space fast-food restaurant with a curved counter, magenta floor and stars outside the windows"
  />
</Figure>

<Figure caption="Original versus snap-blend: Monolith Burger from outside." zoom>
  <img
    src={roomBurgerExterior.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: the exterior of Monolith Burger, a round brown space station with a red sign and a docking arm against stars"
  />
</Figure>

<Figure caption="Original versus snap-blend: magenta desert dunes." zoom>
  <img
    src={roomDesert.src}
    width="1920"
    height="1140"
    alt="Sweep comparison, original versus snap-blend: rolling magenta sand dunes with green and blue rock outcrops under a dark striped sky"
  />
</Figure>

## Upscaled animation overlays

One practical extension is compositing higher-resolution animated elements on top of snap-blend backgrounds.

Because snap-blend produces clean, stable regions, it acts as a reliable base layer. Animated sprites or effects, whether traditionally upscaled or AI-enhanced, can be overlaid without exposing the kinds of boundary artifacts that plagued the original pipeline.

In motion, the eye is far more forgiving. Minor inconsistencies in sprite detail are less noticeable than structural errors in the background. As long as the underlying regions are correct, the combined result reads as coherent.

This also sidesteps one of the hardest problems in AI upscaling: maintaining consistency across frames. By limiting enhancement to overlays and keeping the background deterministic, you avoid compounding temporal artifacts.

The frame is a stack: the deterministic background and its masks stay exact, and only the overlay layer is free to be enhanced.

<VizFigure
  name="Deterministic base with an enhanced overlay layer"
  summary="The snap-blend background and the priority and control masks come from the deterministic renderer and are composited exactly. Animated sprites, upscaled or AI-enhanced, are composited on top as a separate overlay layer, so enhancement never changes the regions the game logic depends on."
>
  <OverlayStackViz class="mx-auto h-auto w-full max-w-2xl" aria-hidden="true" />
</VizFigure>

![Eight-frame walk cycle of the Space Quest hero, Roger Wilco, as native pixel art on a blue background: brown hair, purple shirt, grey trousers, black boots](./walk-cycle-native.png)

![The same eight-frame walk cycle drawn at higher resolution with smoother outlines and no background, ready to overlay on a snap-blend room](./walk-cycle-enhanced.png)

## The future

Once the renderer became structurally reliable, the next question was how far visual enhancement could go without breaking correctness.

AI upscaling looks like an obvious shortcut. Take the original 320x190 output, run it through a modern image model, and get something that looks like a painted remake.

<Figure caption="Original render versus an AI-upscaled painting of the same hangar room." zoom>
  <img
    src={sweepAi.src}
    width="1920"
    height="1140"
    alt="Sweep comparison of the crashed-ship hangar room: the original EGA render on the left, and a photorealistic AI-upscaled version on the right with a metal ship hull, rusted floor plates and a glowing cyan window"
  />
</Figure>

That works for screenshots. It breaks down for a renderer.

SCI0 backgrounds are not just images, they are structured scenes with implicit gameplay constraints. Walkable areas, object boundaries, and interaction zones all depend on the exact shape of regions.

Different colors tell the engine if the avatar should be seen in front or behind them.

![The priority map for the hangar room: flat regions in cyan, blue, magenta and red on black, one colour per depth band, marking which parts of the scene draw in front of or behind the player](./priority-map.png)

White areas block the avatar. Black areas are walkable with no trigger. Any other colour is walkable _and_ triggers an event when walked over.

![The control map for the same room: mostly white, with black bands marking the walkable floor and green patches marking areas that trigger events when walked over](./control-map.png)

AI upscaling works well for screenshots, but renderers require deterministic structural correctness. Walk masks, occlusion boundaries, and interaction zones depend on exact regions. Because generative models can shift those boundaries unpredictably, AI is better treated as a constrained enhancement layer on top of a deterministic renderer.

![An AI-generated photorealistic scene: two red-skinned, red-mohawked alien guards in red uniforms and sunglasses facing a mustached man in a grey suit, with a forest behind them](./ai-upscaled-scene.png)

## The road here

This was strategy v5 out of ten attempts. Strategies v1 through v4 each solved part of the problem — better line expansion, smarter gap detection — but none handled the full space of edge cases. The native render oracle was the breakthrough: instead of trying to predict where gaps would form, just render at native res first and let the result tell you what's correct. v6 through v10 explored alternatives; v10 even improved the metric slightly, but not visibly enough to earn a switch — that detour has [its own write-up](/agi-up-as-a-reference-renderer/). And one successor isn't a snap variant at all: [the anchor-graph upscaler](/omyac-upscaler-anchor-graph/), a separate paradigm that has since won the PIC visual path. `snap-v5-blend` remains the game-runtime strategy, because the anchor-graph renderer doesn't produce the priority and control layers the engine needs.

Sometimes the best algorithm is "do it twice (or ten times) and compare."

<Callout variant="note">
  All original background assets are the property of [Sierra On-Line](https://www.sierragames.com/). Their exact copyright status varies by title and jurisdiction, and this project does not attempt to reinterpret or redistribute those assets beyond rendering them in a different form. If you are interested in these games, the right move is to support the original creators and rights holders wherever those titles are available today.
</Callout>
